# Datasets

This folder is used to host training, alignment, and evaluation dataset loaders, raw files, or preprocessing routines.

## Quick Rules
- Preprocessing scripts and data tokenization tools go here.
- Large raw datasets should be kept locally and are ignored by Git. Only config pointers and small sample files may be committed.
