# Training

Contains supervised fine-tuning (SFT) loops, trainer pipelines, and custom loss functions.

## Focus Areas
- **Spectrum SFT**: Implementing projection mapping target loss.
- Standard cross-entropy sequence-to-sequence SFT.
