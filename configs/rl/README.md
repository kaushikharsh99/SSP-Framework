# Reinforcement Learning Configurations

This folder contains configs for RL training paradigms (e.g. PPO, GRPO, MGPO, DPO).
Configurations will define actor/critic/reference model ratios, KL penalty coefficients, advantage calculation parameters, reward shapes, and rollouts configurations.
