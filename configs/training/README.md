# Training Configurations

This folder contains configurations for training runs (e.g. supervised training, SFT).
Configurations will define batch size, learning rates, scheduler parameters, epochs, optimizer settings, gradient accumulation steps, and mixed precision settings.
