# Model Configurations

This folder contains YAML configuration files for models.
Configurations will define model architecture, hyper-parameters (e.g. number of layers, hidden dimension, spectrum parameters), and pre-trained weights references.
