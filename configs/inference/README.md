# Inference Configurations

This folder contains configurations for inference and generation.
Configurations will define decoding parameters (e.g. temperature, top_p, top_k, repetition penalty), max sequence lengths, and deployment/batching backends.
